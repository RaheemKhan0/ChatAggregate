FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

RUN npx prisma generate

ENV NODE_OPTIONS="--max-old-space-size=4096"

EXPOSE 3000

CMD ["sh", "-c", "npx prisma migrate deploy && npx next dev --webpack"]
